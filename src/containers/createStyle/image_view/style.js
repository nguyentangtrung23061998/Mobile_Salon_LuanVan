import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  touchableOpacity0: {
    position: 'absolute',
    right: 0,
    top: 0,
    zIndex: 1,
    width: 25,
    height: 25,
    justifyContent: 'center',
    alignItems: 'center',
  },

  button0: {
    width: '100%',
    height: '100%',
  },

  image0: {width: '100%', height: '100%'},

  view0: {width: '22%', aspectRatio: 1 / 1},
});

export default styles;
