import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  view0: {
    height: 54,
    width: 54,
    borderRadius: 54 / 2,
    borderColor: '#8ec3ff',
    justifyContent: 'center',
    alignItems: 'center',
  },

  image0: {width: 42, height: 42},
});

export default styles;
